<h1>id: <%= @task.id %> のメッセージ詳細ページ</h1>

<p>詳細: <%= @task.content %></p>

<%= link_to '一覧に戻る', tasks_path %>
<%= link_to '編集', edit_task_path(@task) %>
<%= link_to '削除', @ask, ththod: :delete, data: { confirm: '本当に削除してよろしいですか？' } %>
